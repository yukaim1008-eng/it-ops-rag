你是一位资深AI工程师，我正在做一个“智能IT运维知识库”的RAG高阶检索项目，需要你协助我用Python实现。

## 项目定位
面向企业IT服务台的智能问答系统，能精准回答技术问题并给出可溯源的解决步骤，核心亮点是“高阶检索”。

## 技术栈
- 框架：LlamaIndex或 LangChain 均可
- 向量库：Qdrant（本地或云）
- 嵌入模型：BGE-large（本地部署，免费）
- LLM：云端模型，通过API调用（GPT-4o-mini 或 DeepSeek，由我提供API Key）
- 界面：Streamlit
- 重排序：Cross-encoder（如 bge-reranker-base）

## 整体架构（四层）
### 1. 用户交互层 (main.py)
- Streamlit聊天界面，展示对话历史
- 侧边栏提供高级选项：检索模式（快速/深度推理）、元数据过滤（如系统模块选择）
- 每个回答下方可折叠查看引用来源，并高亮原文片段

### 2. 智能编排层 (orchestrator.py)
- 查询重写器：将用户模糊问题改写成2-3条不同角度的专业检索查询（调用LLM）
- 检索路由器：判断问题类型，决定侧重稠密检索或混合检索
- 多步推理器：初检后若信息不足，自动生成新检索词进行第二次检索，最多2轮

### 3. 高阶检索核心层 (retrieval.py)
- 混合检索器：稠密向量检索 + BM25稀疏检索，结果去重合并
- 元数据过滤器：根据选择的系统模块、文档类型等过滤
- 重排序：粗召回Top20 → Cross-encoder精排 → 取Top3给LLM

### 4. 数据与存储层 (data_loader.py)
- 加载运维文档（Markdown/PDF），进行文本切片
- 用BGE-large生成嵌入，存入Qdrant集合，每条向量附带元数据（系统名、文档类型、时间等）
- 该模块通常作为离线脚本单独运行

## 文件拆分
请严格按以下文件组织，每个文件职责单一：
- `config.py`：集中配置（API Key、模型名、Qdrant地址、文件路径等）
- `data_loader.py`：文档加载、切片、嵌入、入库
- `retrieval.py`：所有检索相关函数（混合检索、过滤、重排序）
- `orchestrator.py`：查询重写、多步推理逻辑，内部调用retrieval和LLM
- `main.py`：Streamlit应用，调用orchestrator
- `utils.py`：通用工具（BM25分词、去重合并、日志等）

## 不需要实现的功能
- 用户登录/注册（直接跳过）
- 但可在侧边栏加一个“当前角色”下拉框，用于演示不同权限的元数据过滤，真实环境可对接企业SSO

## 开发顺序建议
1. 先完成 `config.py` 和 `data_loader.py`，准备好向量数据和配置
2. 再实现 `retrieval.py`，跑通混合检索+重排序
3. 接着开发 `orchestrator.py`，加入查询重写和多步推理
4. 最后用 `main.py` 把所有功能串起来，做好Streamlit界面

## 代码风格要求
- 添加清晰的中文注释，说明关键逻辑
- 所有LLM调用使用统一的接口（从config导入模型名和API参数）
- 检索函数需要返回文档内容和元数据，便于界面展示来源
